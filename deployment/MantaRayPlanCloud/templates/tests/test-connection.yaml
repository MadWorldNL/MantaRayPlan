apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "MantaRayPlanCloud.fullname" . }}-test-connection"
  labels:
    {{- include "MantaRayPlanCloud.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "MantaRayPlanCloud.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
