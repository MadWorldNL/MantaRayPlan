apiVersion: v1
kind: Service
metadata:
  name: {{ include "MantaRayPlanCloud.fullname" . }}
  labels:
    {{- include "MantaRayPlanCloud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "MantaRayPlanCloud.selectorLabels" . | nindent 4 }}
